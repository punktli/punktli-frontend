{{component (concat 'navigation-' this.navigation)}}
<div id="main" class="container-fluid">
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}
  {{outlet}}
  <NavigationFooter @show={{showFooter}}></NavigationFooter>
</div>